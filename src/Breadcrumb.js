function Breadcrumb({catName}){
    return(
        <nav className="Breadcrumb">
            <div>root - {catName}</div>
            {/* <div>{title.name}</div> */}
            {/* <div>yelloCat</div> */}
        </nav>
    )
}

export default Breadcrumb